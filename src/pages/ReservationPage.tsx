import { useState } from 'react';
import { CalendarDays, Phone } from 'lucide-react';
import { restaurant, imagePaths } from '../data/restaurant';
import { ButtonLink, DemoSubmitButton, PageHero } from '../components/ui';

export default function ReservationPage() { const [message, setMessage] = useState(false); return <><PageHero eyebrow="Réservation" title="Préparez votre visite" text="Réservez auprès du restaurant en ligne ou par téléphone. Le formulaire ci-dessous illustre la future expérience." image={imagePaths.interior02} /><section className="section"><div className="container reservation-grid"><aside><h2>Réserver directement</h2><p>Pour une réservation confirmée aujourd’hui, utilisez le site actuel ou appelez le restaurant.</p><ButtonLink to={restaurant.reservationUrl}><CalendarDays /> Réserver en ligne</ButtonLink><ButtonLink to={restaurant.phone.href} variant="secondary"><Phone /> {restaurant.phone.display}</ButtonLink></aside><form className="form-card" onSubmit={event => { event.preventDefault(); setMessage(true); }}><h2>Demande de réservation</h2><p className="form-note">Démonstration uniquement : aucune donnée n’est envoyée ni enregistrée.</p><div className="form-grid"><label>Nom<input required name="name" autoComplete="name" /></label><label>Téléphone<input required name="phone" type="tel" autoComplete="tel" /></label><label>E-mail<input required name="email" type="email" autoComplete="email" /></label><label>Date<input required name="date" type="date" /></label><label>Heure<input required name="time" type="time" /></label><label>Nombre de personnes<select name="guests" defaultValue="2">{[1,2,3,4,5,6,7,8].map(value => <option key={value}>{value}</option>)}</select></label><label className="full-field">Demande particulière<textarea name="request" rows={4} /></label></div><DemoSubmitButton>Prévisualiser la demande</DemoSubmitButton>{message && <p className="demo-message" role="status">L’intégration de réservation en ligne sera connectée dans le site final. Pour réserver maintenant, utilisez les options directes ci-dessus.</p>}</form></div></section></>; }
